#!/usr/bin/env python3
"""Drill"""

from lib.table import Table
from lib.utils import BALL, HBALL, NFX, PFX, NFY, PFY, NHX, PHX, NHY, PHY, N1X, P1X, N1Y, P1Y


# ┌─────────────────────────────────────────────────────────────────────────────
# │ Setup
# └─────────────────────────────────────────────────────────────────────────────
table_std = Table()
table_dyn = Table()


# ┌─────────────────────────────────────────────────────────────────────────────
# │ Standard
# └─────────────────────────────────────────────────────────────────────────────
balls = {
    'cb': [(3.5, 1), (3.5, -1)],
    'cbg': [(3.5 - BALL, 1), (3.5 + BALL, 1), (3.5 - BALL, -1), (3.5 + BALL, -1)],
    'b1': [(3, PFY), (3, NFY)],
    'b2': [(2, PFY), (2, NFY)],
    'b3': [(1, PFY), (1, NFY)],
    'b4': [(0, PFY), (0, NFY)],
    'b5': [(-1, PFY), (-1, NFY)],
    'b6': [(-2, PFY), (-2, NFY)],
    'b7': [(-3, PFY), (-3, NFY)],
    'b8': [(NFX, PFY), (NFX, NFY)],
    'b9': [(NFX, 1), (NFX, -1)],
    'b10': [(NFX, 0)],
    'b15': [(PHX, 0)],
}
table_std.add_balls(balls)
table_std.save(__file__)


# # ┌─────────────────────────────────────────────────────────────────────────────
# # │ Dynamic
# # └─────────────────────────────────────────────────────────────────────────────
# dyn_balls = {
#     'cb': ([], []),
#     'gb': ([], []),
#     'b1': ([], []),
#     'b2': ([], []),
#     'b3': ([], []),
#     'b4': ([], []),
#     'b5': ([], []),
#     'b6': ([], []),
#     'b7': ([], []),
#     'b8': ([], []),
#     'b9': ([], []),
#     'b10': ([], []),
#     'b11': ([], []),
#     'b12': ([], []),
#     'b13': ([], []),
#     'b14': ([], []),
#     'b15': ([], []),
# }
# table_dyn.add_dyn_balls(dyn_balls)
# table_dyn.save_dyn(__file__)
